import React from "react";
import { useForm } from "react-hook-form";

const Login = () => {
  const {} = useForm();
  return (
    <React.Fragment>
      <h1>Login</h1>
    </React.Fragment>
  );
};

export default Login;
